{
  "name": "populate-data",
  "version": "1.0.0",
  "description": "Lambda function to populate DynamoDB with initial data",
  "main": "index.js",
  "dependencies": {
    "aws-sdk": "^2.1400.0"
  },
  "scripts": {
    "zip": "zip -r ../populate_data.zip ."
  }
}
